export interface IconProps {
  width: number;
  height: number;
  color?: string;
}

export interface LoginInfo {
  email: string;
  password: string;
}

export interface RegiserInfo {
  username: string;
  email: string;
  password: string;
}
